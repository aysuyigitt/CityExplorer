@model List<ResultEventDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="page-content">
    <div class="container-fluid">

        <!-- start page title -->
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Etkinlik Listesi</h4>
                        <p class="card-subtitle mb-4">
                            Etkinlik ile ilgili işlemlerin gerçekleştireleceği sayfadır.
                        </p>
                        <div class="table-responsive">
                            <table class="table mb-0">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Etkinlik Adı</th>
                                        <th>Etkinlik Açıklaması</th>
                                        <th>Etkinlik Resmi</th>
                                        <th>Etkinlik Konumu</th>
                                        <th>Etkinlik R-Tarihi</th>
                                        <th>Sil</th>
                                        <th>Güncelle</th>
                                      
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td>@count</td>
                                            <td>@item.Name</td>
                                            <td>@item.Description</td>
                                            <td>@item.ImageUrl</td>
                                            <td>@item.Location</td>
                                            <td>@item.StartDate</td>
                                            <td><a href="/Admin/AdminEvent/RemoveEvent/@item.EventID" class="btn btn-outline-danger">Sil</a></td>
                                            <td><a href="/Admin/AdminEvent/UpdateEvent/@item.EventID" class="btn btn-outline-success">Güncelle</a></td>


                                        </tr>
                                    }
                                </tbody>
                            </table>
                            <td><a href="/Admin/AdminEvent/CreateEvent" class="btn btn-outline-primary">Yeni Etkinlik Girişi</a></td>
                        </div>

                    </div>

                </div>


            </div>


        </div>



    </div>
</div>







